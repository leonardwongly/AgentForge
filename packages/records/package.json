{
  "name": "@agentforge/records",
  "version": "1.1.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "cd ../.. && vitest run packages/records/src/*.test.ts"
  },
  "dependencies": {
    "@agentforge/core": "workspace:*",
    "@agentforge/security": "workspace:*"
  }
}
